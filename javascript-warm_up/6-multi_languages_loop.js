#!/usr/bin/node
// Prints 3 lines, using array and string with a loop.

const arrayPrint = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const line of arrayPrint) {
    console.log(line);
}